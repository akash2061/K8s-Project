all:
  children:
    k3s_servers:
      hosts:
        k3s_node:
          ansible_host: "{{ k3s_host }}"
          ansible_user: "{{ k3s_user }}"
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
